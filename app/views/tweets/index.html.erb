<p style="color: green"><%= notice %></p>

<h1>Tweets</h1>

<div id="tweets">
  <% @tweets.each do |tweet| %>
    <%= render tweet %>
    <p>
      <%= link_to "Show this tweet", user_tweet_path(@user, tweet) %>
    </p>
  <% end %>
</div>

<%= link_to "New tweet", new_user_tweet_path %>
